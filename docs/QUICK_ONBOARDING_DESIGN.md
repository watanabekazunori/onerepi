# 初回3分体験（Quick Onboarding）設計書

## 🎯 ゴール
初回起動から3分以内に「もう一回使ってもいいかも」と思わせる。
**思考代行体験**を最優先。

---

## 📱 画面フロー図

```
[App Start]
    │
    ▼
┌─────────────────────────────────────┐
│  Screen 0: CatchScreen              │
│  「今日のごはん、もう決めなくていいよ」│
│  [決めてほしい]                      │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  Screen 1: Question1Screen          │
│  「疲れて帰った夜、どうしたい？」     │
│  (A) とにかく早く終わらせたい        │
│  (B) 美味しいもので回復したい        │
└─────────────────────────────────────┘
    │ 選択と同時に自動遷移
    ▼
┌─────────────────────────────────────┐
│  Screen 2: Question2Screen          │
│  「見たことない調味料があったら？」   │
│  (A) 今回はスルー                    │
│  (B) ちょっと気になる                │
└─────────────────────────────────────┘
    │ 選択と同時に自動遷移
    ▼
┌─────────────────────────────────────┐
│  Screen 3: ThinkingScreen           │
│  「あなたの代わりに考えてるよ…」     │
│  [フライパンアニメーション]          │
│  (5〜10秒)                          │
└─────────────────────────────────────┘
    │ 自動遷移
    ▼
┌─────────────────────────────────────┐
│  Screen 4: TodayMealScreen          │
│  「🍽 今日のごはん」                 │
│  主菜 + 副菜 + バッジ + 理由1行      │
│  [この献立でいく]                    │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  Screen 5: WeekPreviewScreen        │
│  「今週はこんな感じになるよ」        │
│  月〜金の雰囲気表示                  │
│  [今日はこれでOK]                    │
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  Screen 6: CompleteScreen           │
│  「今日はこれでいこう」              │
│  → ホーム画面へ自動遷移              │
└─────────────────────────────────────┘
```

---

## 📝 UI文言一覧

### Screen 0: CatchScreen
- メインコピー: 「今日のごはん、\nもう決めなくていいよ」
- CTA: 「決めてほしい」

### Screen 1: Question1Screen
- 説明文: 「いくつか答えてくれたら\n今日と今週のごはんを決めるよ」
- 質問: 「疲れて帰った夜、どうしたい？」
- 選択肢A: 「とにかく早く終わらせたい」
- 選択肢B: 「美味しいもので回復したい」

### Screen 2: Question2Screen
- 質問: 「見たことない調味料があったら？」
- 選択肢A: 「今回はスルー」
- 選択肢B: 「ちょっと気になる」

### Screen 3: ThinkingScreen
- メッセージ: 「あなたの代わりに考えてるよ…」

### Screen 4: TodayMealScreen
- 見出し: 「🍽 今日のごはん」
- 主菜ラベル: 「主菜」
- 副菜ラベル: 「副菜」
- 理由フォーマット: 「{理由}」
- CTA: 「この献立でいく」

### Screen 5: WeekPreviewScreen
- 見出し: 「今週はこんな感じになるよ」
- 曜日テキスト:
  - 月: 「定番ワンパン」
  - 火: 「余り活用」
  - 水: 「ちょい気分転換」
  - 木: 「楽め」
  - 金: 「おまかせ」
- CTA: 「今日はこれでOK」

### Screen 6: CompleteScreen
- メイン: 「今日はこれでいこう」
- サブ: 「次は、疲れてる日はもっと楽にできるよ」

---

## 🧠 内部ロジック

### 暫定タイプ判定ロジック

```typescript
type QuickAnswer = {
  q1: 'A' | 'B'; // 疲れた夜
  q2: 'A' | 'B'; // 見知らぬ調味料
};

// 4パターンから暫定タイプを推測
const inferQuickType = (answers: QuickAnswer): FoodPsychologyType => {
  const { q1, q2 } = answers;

  if (q1 === 'A' && q2 === 'A') {
    // 効率重視 & 安定志向 → smart_balancer
    return 'smart_balancer';
  }
  if (q1 === 'A' && q2 === 'B') {
    // 効率重視 & 探求志向 → balanced (効率的だが新しいものも)
    return 'balanced';
  }
  if (q1 === 'B' && q2 === 'A') {
    // 回復重視 & 安定志向 → healing_gourmet
    return 'healing_gourmet';
  }
  if (q1 === 'B' && q2 === 'B') {
    // 回復重視 & 探求志向 → trend_hunter
    return 'trend_hunter';
  }

  return 'balanced';
};
```

### 献立選択ロジック

1. 暫定タイプに基づき、type_specific + universal から選択
2. 主菜: 15分以内、難易度easy優先
3. 副菜: 主菜と食材が被らない、10分以内
4. 食材数: 合計6品以下を目指す

### 理由生成ロジック

```typescript
const generateQuickReason = (
  q1: 'A' | 'B',
  q2: 'A' | 'B'
): string => {
  if (q1 === 'A' && q2 === 'A') {
    return '効率派っぽいから、パパッと作れるやつ';
  }
  if (q1 === 'A' && q2 === 'B') {
    return '時短だけど、ちょっと新鮮なやつ';
  }
  if (q1 === 'B' && q2 === 'A') {
    return '今日は回復モード、安心の定番で';
  }
  if (q1 === 'B' && q2 === 'B') {
    return '元気出したいなら、ちょっと冒険もアリ';
  }
  return 'あなたに合いそうなやつ';
};
```

---

## 🧩 コンポーネント構成

```
src/
├── screens/
│   └── quickOnboarding/
│       ├── index.ts                    # エクスポート
│       ├── QuickOnboardingNavigator.tsx # Stack Navigator
│       ├── CatchScreen.tsx             # Screen 0
│       ├── Question1Screen.tsx         # Screen 1
│       ├── Question2Screen.tsx         # Screen 2
│       ├── ThinkingScreen.tsx          # Screen 3
│       ├── TodayMealScreen.tsx         # Screen 4
│       ├── WeekPreviewScreen.tsx       # Screen 5
│       └── CompleteScreen.tsx          # Screen 6
├── lib/
│   └── quickOnboarding.ts              # ロジック（タイプ判定・献立選択）
└── types/
    └── quickOnboarding.ts              # 型定義
```

---

## 📊 State / Flow 設計

### QuickOnboardingContext

```typescript
interface QuickOnboardingState {
  // 回答
  answers: {
    q1?: 'A' | 'B';
    q2?: 'A' | 'B';
  };

  // 推測結果
  inferredType?: FoodPsychologyType;

  // 生成された献立
  todayMeal?: {
    main: Recipe;
    side: Recipe;
    reason: string;
  };

  // 週の雰囲気
  weekVibes?: {
    mon: string;
    tue: string;
    wed: string;
    thu: string;
    fri: string;
  };
}
```

### フロー制御

1. **CatchScreen** → CTAタップで Question1 へ
2. **Question1** → 選択と同時に answers.q1 保存 → Question2 へ自動遷移
3. **Question2** → 選択と同時に answers.q2 保存 → Thinking へ自動遷移
4. **Thinking** →
   - inferredType を計算
   - todayMeal を生成
   - weekVibes を生成
   - 5秒後に TodayMeal へ自動遷移
5. **TodayMeal** → CTAタップで WeekPreview へ
6. **WeekPreview** → CTAタップで Complete へ
7. **Complete** →
   - AsyncStorage に保存（quickOnboardingCompleted: true）
   - 今日の献立をストレージに保存
   - 2秒後にホーム画面へ自動遷移

---

## 🎨 デザイン方針

### カラー
- 背景: 温かみのあるクリーム系 (#FFF8F0)
- プライマリ: ブランドオレンジ (#FF8C00)
- テキスト: ダークグレー (#1F2937)

### タイポグラフィ
- メインコピー: 28px, Bold
- 質問文: 20px, Medium
- 選択肢: 18px, Regular
- 説明文: 14px, Regular

### アニメーション
- 画面遷移: fade (300ms)
- 選択肢タップ: scale + fade (200ms)
- ローディング: rotate (continuous)
- **Web対応のため useNativeDriver: false**

---

## 📱 レスポンシブ対応

- 最小幅: 320px
- 最大コンテンツ幅: 400px
- 縦長画面: コンテンツ中央配置
- 横長画面: 左右マージン自動調整
